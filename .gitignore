# Rust / general
/firmware/target/
/target/
Cargo.lock

# OS
.DS_Store

# Deploy artifacts / archives
*.zip
*.tar.gz

# SSH keys (do NOT commit private keys)
scripts/id_rsa_*
!scripts/id_rsa_*.pub
*.pem
*.key
