[target.armv7-unknown-linux-gnueabihf]
linker = "arm-linux-gnueabihf-gcc"

# If you prefer a different target or linker, adjust accordingly. This file is a helper
# for cross-compilation and is ignored when building directly on the Pi.
